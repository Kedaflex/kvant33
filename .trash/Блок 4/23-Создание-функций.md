# Урок 23: Создание функций

> [!info] 🎯 Цель урока  
> После этого урока ты сможешь создавать свои функции с помощью def﻿, вызывать их и передавать в них данные.

## 📚 Что понадобится

-  Компьютер с Python
    
-  Предыдущий урок: [[22-Контрольная-3|Урок 22: Контрольная работа]]
    

---

## 🔥 Разминка (3 мин)

> [!question] Вспомни  
> Что такое функция input()﻿ и как она работает?

<details> <summary>Ответ</summary> [input()](pplx://action/translate) — встроенная функция Python, которая запрашивает данные у пользователя с клавиатуры и возвращает их как строку. </details>

---

## 📖 Теория (7 мин)

## Зачем нужны функции?

Функции позволяют:

- Не повторять один и тот же код много раз
    
- Делать программу читаемой и структурированной
    
- Переиспользовать код с разными данными
    

Функция — это именованный блок кода, который можно вызывать многократно.

## Синтаксис

```python
# Создание функции
def greet(name):
    print(f"Привет, {name}!")

# Вызов функции
greet("Иван")    # Привет, Иван!
greet("Мария")   # Привет, Мария!
```

## Как это работает?

1. def﻿ объявляет имя функции и её параметры (в скобках).
    
2. Тело функции идёт с отступом после двоеточия.
    
3. Функция вызывается по имени с круглыми скобками: имя_функции(аргументы)﻿.
    
4. Параметры — это переменные, которые функция получает при вызове.
    

> [!warning] Частая ошибка  
> Забывать скобки при вызове функции: greet﻿ вместо greet("Иван")﻿. Без скобок функция не выполнится!

---

## 💻 Практика (15 мин)

## Задание: «Мои первые функции»

**Что делаем:** создаём несколько простых функций и вызываем их.

## Шаг 1: Создай файл

```text
Создай новый файл: uroк23_sozdanie_funktsiy.py
```

## Шаг 2: Напиши код

```python
# Функция приветствия
def greet(name):
    print(f"Привет, {name}! Как дела?")

# Функция расчёта площади
def square_side(side):
    area = side * side
    print(f"Площадь квадрата со стороной {side}: {area}")

# Функция вывода таблицы умножения
def multiply_table(number):
    print(f"Таблица умножения на {number}:")
    for i in range(1, 11):
        print(f"{number} × {i} = {number * i}")

# Тестируем функции
greet("Иван")
square_side(5)
multiply_table(3)
```

## Шаг 3: Запусти программу

Убедись, что все три функции работают и выводят правильные результаты.

## Шаг 4: Создай свою функцию

Напиши функцию, которая принимает два числа и выводит их сумму, разность, произведение и частное.

## Шаг 5: Вызови все функции

Добавь вызовы всех своих функций с разными данными.

text

`Ожидаемый результат: Программа последовательно вызывает функции и выводит их результаты.`

> [!success] Готово!  
> Если твои функции работают и выводят правильные результаты, ты освоил создание функций.

---

## ✅ Проверь себя (5 мин)

## Твой код должен:

-  Иметь минимум 2–3 функции с параметрами
    
-  Корректно вызывать функции с аргументами
    
-  Использовать отступы внутри функций
    
-  Работать без синтаксических ошибок
    

## Ответь на вопросы:

1. В чём разница между созданием функции (def﻿) и её вызовом?
    
2. Что такое параметр функции и как он отличается от аргумента?
    

<details> <summary>Ответы</summary>

1. Создание (def имя():﻿) определяет функцию, вызов (имя(данные)﻿) выполняет её код.
    
2. Параметр — это переменная в определении функции (def func(x):﻿), аргумент — это значение, которое передаётся при вызове (func(5)﻿).
    

</details>

---

## ⭐ Бонус (если осталось время)

> [!tip] Дополнительное задание  
> Создай функцию is_even(number)﻿, которая принимает число и возвращает True﻿, если оно чётное, и False﻿, если нечётное. Протестируй её на нескольких числах.

---

## 🔗 Навигация

◀️ Предыдущий: [[22-Контрольная-3|Урок 22: Контрольная работа]]  
▶️ Следующий: [[24-Параметры|Урок 24: Параметры]]  
🏠 [[00 - Главная|Вернуться к оглавлению]]

---

## 📝 Мои заметки

[Место для заметок ученика]