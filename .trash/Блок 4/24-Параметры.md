# Урок 24: Параметры

> [!info] 🎯 Цель урока  
> После этого урока ты сможешь передавать функции несколько параметров, использовать параметры по умолчанию и понимать разницу между обязательными и необязательными параметрами.

## 📚 Что понадобится

-  Компьютер с Python
    
-  Предыдущий урок: [[23-Создание-функций|Урок 23: Создание функций]]
    

---

## 🔥 Разминка (3 мин)

> [!question] Вспомни  
> Как вызвать функцию, которая принимает один параметр?

<details> <summary>Ответ</summary> [func_name("значение")](pplx://action/translate) или [func_name(42)](pplx://action/translate) — в скобках передаётся аргумент. </details>

---

## 📖 Теория (7 мин)

## Несколько параметров

Функция может принимать несколько параметров через запятую:

```python
def full_name(first_name, last_name):
    print(f"Полное имя: {first_name} {last_name}")

full_name("Иван", "Петров")  # Полное имя: Иван Петров
```

## Параметры по умолчанию

Можно задать значения по умолчанию — тогда их можно не передавать:

```python
def greet(name, greeting="Привет"):
    print(f"{greeting}, {name}!")

greet("Иван")                    # Привет, Иван!
greet("Мария", "Добрый день")    # Добрый день, Мария!
```

## Порядок параметров

Обязательные параметры идут первыми, параметры по умолчанию — последними:

```python
def info(name, age=14, city="Москва"):
    print(f"{name}, {age} лет, {city}")
```

> [!warning] Частая ошибка  
> Пытаться передать аргументы в неправильном порядке или забыть, что обязательные параметры должны быть первыми.

---

## 💻 Практика (15 мин)

## Задание: «Расширенные функции»

**Что делаем:** создаём функции с несколькими параметрами и значениями по умолчанию.

## Шаг 1: Создай файл

```text
Создай новый файл: uroк24_parametry.py
```

## Шаг 2: Напиши код

```python
# Функция с двумя параметрами
def rectangle_area(width, height):
    area = width * height
    print(f"Площадь прямоугольника {width}×{height} = {area}")

# Функция с параметрами по умолчанию
def student_info(name, grade=8, school="Школа №1"):
    print(f"{name}, {grade} класс, {school}")

# Функция с тремя параметрами
def calculate(a, b, operation="+"):
    if operation == "+":
        return a + b
    elif operation == "-":
        return a - b
    elif operation == "*":
        return a * b
    else:
        return "Неизвестная операция"

# Тестируем
rectangle_area(5, 3)
student_info("Иван")
student_info("Мария", 9, "Лицей №2")
result = calculate(10, 5, "*")
print(f"Результат: {result}")
```

## Шаг 3: Запусти программу

Проверь все варианты вызовов функций.

## Шаг 4: Создай свою функцию

Напиши функцию draw_rectangle(width, height, symbol="*")﻿, которая выводит прямоугольник из символов заданной ширины и высоты.

## Шаг 5: Протестируй с разными параметрами

Вызови свою функцию с разными значениями: draw_rectangle(5, 3)﻿, draw_rectangle(4, 2, "#")﻿.

text

`Ожидаемый результат: Все функции работают с обязательными и необязательными параметрами.`

> [!success] Готово!  
> Если функции принимают параметры правильно и работают с разными вариантами вызовов, параметры освоены.

---

## ✅ Проверь себя (5 мин)

## Твой код должен:

-  Иметь функции с 2+ обязательными параметрами
    
-  Иметь функции с параметрами по умолчанию
    
-  Корректно вызываться разными способами
    
-  Работать без ошибок
    

## Ответь на вопросы:

1. Можно ли вызвать функцию func(a=1, b=2)﻿ вместо func(1, 2)﻿?
    
2. Почему нельзя поставить обязательный параметр после параметра по умолчанию?
    

<details> <summary>Ответы</summary>

1. Да, можно явно указывать имена параметров: func(a=1, b=2)﻿.
    
2. Потому что Python не поймёт, какой аргумент к какому параметру относится. Обязательные всегда первыми!
    

</details>

---

## ⭐ Бонус (если осталось время)

> [!tip] Дополнительное задание  
> Создай функцию format_price(price, currency="руб")﻿, которая форматирует цену: format_price(1234.56)﻿ → "1234.56 руб"﻿, format_price(999, "$") ﻿ → "999 $"﻿.

---

## 🔗 Навигация

◀️ Предыдущий: [[23-Создание-функций|Урок 23: Создание функций]]  
▶️ Следующий: [[25-Возврат-значений|Урок 25: Возврат значений]]  
🏠 [[00 - Главная|Вернуться к оглавлению]]

---

## 📝 Мои заметки

[Место для заметок ученика]