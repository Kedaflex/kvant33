# Урок 27: Практика — Библиотека функций

> [!info] 🎯 Цель урока  
> После этого урока ты создашь свой модуль (библиотеку) с полезными функциями в отдельном файле и научишься импортировать его в другую программу.[](https://docs.python.org/3/tutorial/modules.html)​

## 📚 Что понадобится

-  Компьютер с Python
    
-  Предыдущие уроки: [[23-Создание-функций|Урок 23: Создание функций]], [[24-Параметры|Урок 24: Параметры]], [[25-Возврат-значений|Урок 25: Возврат значений]], [[26-Область-видимости|Урок 26: Область видимости]]
    

---

## 🔥 Разминка (3 мин)

> [!question] Вспомни  
> Как вызвать функцию из модуля math﻿, например, корень числа?

<details> <summary>Ответ</summary> Нужно импортировать модуль и вызвать функцию через точку, например: [import math](pplx://action/translate) [math.sqrt(16)](pplx://action/translate).[2][4] </details>

---

## 📖 Теория (7 мин)

## Что такое модуль?

Модуль — это обычный файл .py﻿, в котором лежит набор функций и переменных.  
Его можно «подключить» в другой программе через import﻿ и использовать как библиотеку.[](https://docs.python.org/3/tutorial/modules.html)​

Создание модуля:

1. Создаёшь файл, например my_utils.py﻿
    
2. Пишешь в нём функции
    
3. В другом файле делаешь import my_utils﻿
    
4. Вызываешь функции как my_utils.func_name()﻿
    

## Простой пример

Файл my_utils.py﻿:

```python
def add(a, b):
    return a + b

def is_even(x):
    return x % 2 == 0
```

Другой файл main.py﻿:

```python
import my_utils

print(my_utils.add(2, 3))          # 5
print(my_utils.is_even(4))        # True
```

Можно импортировать только нужные функции: from my_utils import add﻿.[](https://datasciocean.com/en/python-tutorial/python-module/)​

> [!warning] Частая ошибка  
> Файл модуля и файл программы должны лежать в одной папке, иначе Python не найдёт модуль.[](https://www.w3schools.com/python/python_modules.asp)​

---

## 💻 Практика (15 мин)

## Задание: «Своя библиотека функций»

**Что делаем:** создаём модуль с полезными функциями и используем его в другой программе.

## Шаг 1: Создай модуль с функциями

Создай файл:
```text
Создай новый файл: my_library.py
```

## Шаг 2: Напиши код в my_library.py

```python
# Твоя библиотека функций
def add(a, b):
    return a + b

def sub(a, b):
    return a - b

def mul(a, b):
    return a * b

def div(a, b):
    if b == 0:
        return "Ошибка: деление на ноль!"
    return a / b

def is_even(x):
    return x % 2 == 0

def average(numbers):
    if len(numbers) == 0:
        return 0
    return sum(numbers) / len(numbers)

```
## Шаг 3: Создай основной файл

Создай второй файл:

```text
Создай новый файл: uroк27_biblioteka_funktsiy.py
```

## Шаг 4: Используй модуль в основном файле

```python
import my_library

print("Тест библиотеки функций\n")

print("Сложение 10 + 5 =", my_library.add(10, 5))
print("Вычитание 10 - 5 =", my_library.sub(10, 5))
print("Умножение 10 * 5 =", my_library.mul(10, 5))
print("Деление 10 / 5 =", my_library.div(10, 5))
print("Деление на ноль:", my_library.div(10, 0))

print("\nПроверка чётности:")
for n in [1, 2, 3, 4, 10]:
    print(f"{n} чётное? {my_library.is_even(n)}")

print("\nСреднее значение списка [1, 2, 3, 4, 5]:")
print(my_library.average([1, 2, 3, 4, 5]))

```
## Шаг 5: Запусти основной файл

Убедись, что Python видит my_library.py﻿ и все функции работают корректно.

text

`Ожидаемый результат: Основная программа использует функции из отдельного файла-модуля.`

> [!success] Готово!  
> Если всё работает и функции импортируются, ты создал свою первую библиотеку.

---

## ✅ Проверь себя (5 мин)

## Твой код должен:

-  Иметь отдельный файл-модуль с функциями
    
-  Импортировать модуль в основном файле через import﻿
    
-  Вызывать функции как module_name.function_name()﻿[](https://www.datacamp.com/tutorial/modules-in-python)​
    
-  Работать без ошибок импорта
    

## Ответь на вопросы:

1. Зачем выносить функции в отдельный модуль?
    
2. В чём разница между import my_library﻿ и from my_library import add﻿?
    

<details> <summary>Ответы</summary>

1. Чтобы переиспользовать функции в разных программах и не копировать код; для удобства и порядка в проекте.[](https://www.geeksforgeeks.org/python/python-modules/)​
    
2. import my_library﻿ подключает весь модуль и функции вызываются как my_library.add()﻿, а from my_library import add﻿ импортирует только функцию add﻿, и её можно вызывать напрямую.[](https://www.datacamp.com/tutorial/modules-in-python)​
    

</details>

---

## ⭐ Бонус (если осталось время)

> [!tip] Дополнительное задание  
> Добавь в my_library.py﻿ функцию grade_to_text(mark)﻿, которая принимает оценку (5, 4, 3, 2) и возвращает текст: "отлично"﻿, "хорошо"﻿, "удовлетворительно"﻿, "неудовлетворительно"﻿. Используй её в основном файле.

---

## 🔗 Навигация

◀️ Предыдущий: [[26-Область-видимости|Урок 26: Область видимости]]  
▶️ Следующий: [[28-Контрольная-4|Урок 28: Контрольная работа]]  
🏠 [[00 - Главная|Вернуться к оглавлению]]

---

## 📝 Мои заметки

[Место для заметок ученика]