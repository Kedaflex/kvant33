# 📚 Шпаргалки по Python

> [!info] Как пользоваться  
> Эта страница — твоя быстрая справка. Если забыл синтаксис какой-то команды, посмотри сюда. Все примеры рабочие, можешь скопировать и вставить в свой код.

---

## 📌 БАЗОВЫЙ СИНТАКСИС

## Вывод текста

```python
print("Привет, мир!")
print("Имя:", name)
print(x, y, z)  # несколько значений
```

## Ввод данных

```python
name = input("Как тебя зовут? ")
age = int(input("Возраст: "))  # превращаем в число
height = float(input("Рост (м): "))  # число с точкой
```
## Комментарии

```python
# Это однострочный комментарий
x = 5  # комментарий в конце строки
```

---

## 🔢 ПЕРЕМЕННЫЕ И ТИПЫ ДАННЫХ

## Создание переменных

```python
name = "Иван"      # строка (str)
age = 14           # целое число (int)
height = 1.75      # число с точкой (float)
is_student = True  # логический (bool)
```

## Проверка типа

```python
type(name)        # lass 'str'>
type(age)         # lass 'int'>
type(height)      # lass 'float'>
type(is_student)  # lass 'bool'>
```

## Преобразование типов

```python
int("123")        # "123" → 123
str(14)           # 14 → "14"
float("3.14")     # "3.14" → 3.14
int(3.9)          # 3.9 → 3 (отсекает дробь)
```
## ➕ АРИФМЕТИЧЕСКИЕ ОПЕРАЦИИ

```python
a + b      # сложение
a - b      # вычитание
a * b      # умножение
a / b      # деление (с дробью)
a // b     # целочисленное деление
a % b      # остаток от деления
a ** b     # возведение в степень
```
## Примеры

```python
15 + 3     # 18
15 - 3     # 12
15 * 3     # 45
15 / 3     # 5.0
15 // 3    # 5
15 % 3     # 0
2 ** 3     # 8
```
## Сокращённая запись

```python
x += 5     # x = x + 5
x -= 3     # x = x - 3
x *= 2     # x = x * 2
x /= 2     # x = x / 2
````

---

## 📝 РАБОТА СО СТРОКАМИ

## Конкатенация (склеивание)

```python
"Привет" + " " + "мир"         # "Привет мир"
"Число: " + str(42)            # "Число: 42"
```

## f-строки (подстановка переменных)

```python
name = "Иван"
age = 14
f"Меня зовут {name} и мне {age} лет."
f"Сумма: {10 + 5}"
```

## Методы строк

```python
text = "Привет"
text.lower()           # "привет" (в нижний регистр)
text.upper()           # "ПРИВЕТ" (в верхний регистр)
text.replace("е", "о") # "Приво" (заменить)
len(text)              # 6 (длина строки)
```

---

## 🔀 УСЛОВИЯ (if-else)

## Базовая структура

```python
if условие:
    # код, если условие истинно
else:
    # код, если условие ложно
```

## Несколько условий

```python
if x > 10:
    print("Большое число")
elif x > 5:
    print("Среднее число")
else:
    print("Маленькое число")
```

## Операторы сравнения

```python
a == b      # равно
a != b      # не равно
a > b       # больше
a < b       # меньше
a >= b      # больше или равно
a <= b      # меньше или равно
```

## Логические операторы

```python
x > 5 and x < 10      # И (оба условия должны быть истинны)
x < 0 or x > 100      # ИЛИ (хотя бы одно условие истинно)
not x > 10            # НЕ (инверсия условия)
```
---

## 🔁 ЦИКЛЫ

## Цикл for

```python
for i in range(5):
    print(i)
# выводит 0, 1, 2, 3, 4

for i in range(1, 6):
    print(i)
# выводит 1, 2, 3, 4, 5

for i in range(0, 10, 2):
    print(i)
# выводит 0, 2, 4, 6, 8
```

## Цикл while

```python
count = 0
while count < 5:
    print(count)
    count += 1
# выводит 0, 1, 2, 3, 4
```

## break и continue

```python
for i in range(10):
    if i == 5:
        break      # выход из цикла
    print(i)

for i in range(10):
    if i == 5:
        continue   # пропуск текущей итерации
    print(i)
```

---

## 📊 СПИСКИ

## Создание и доступ

```python
numbers = [1, 2, 3, 4, 5]
names = ["Иван", "Мария", "Петр"]
empty = []

numbers[0]     # 1 (первый элемент, индекс начинается с 0)
numbers[2]     # 3 (третий элемент)
numbers[-1]    # 5 (последний элемент)
```

## Изменение списка

```python
lst = [1, 2, 3]
lst.append(4)           # добавить в конец: [1, 2, 3, 4]
lst.insert(1, 10)       # вставить на позицию 1: [1, 10, 2, 3, 4]
lst.remove(2)           # удалить элемент 2: [1, 10, 3, 4]
lst.pop()               # удалить последний: [1, 10, 3]
lst.pop(0)              # удалить первый: [10, 3]

```

## Функции со списками

```python
len(lst)        # количество элементов
max(lst)        # максимальное значение
min(lst)        # минимальное значение
sum(lst)        # сумма всех элементов
```

## Перебор списка

```python
for num in [1, 2, 3]:
    print(num)

for i in range(len(lst)):
    print(lst[i])
```

## Срезы (slicing)

```python
lst = [0, 1, 2, 3, 4, 5]
lst[1:4]        # [1, 2, 3] (от индекса 1 до 3)
lst[:3]         # [0, 1, 2] (от начала до индекса 2)
lst[3:]         # [3, 4, 5] (от индекса 3 до конца)
lst[::2]        # [0, 2, 4] (каждый второй элемент)
lst[::-1]       # [5, 4, 3, 2, 1, 0] (в обратном порядке)
```

---

## 📖 СЛОВАРИ

## Создание и доступ

```python
person = {
    "name": "Иван",
    "age": 14,
    "city": "Москва"
}

person["name"]      # "Иван"
person["age"]       # 14
```
## Изменение словаря

```python
person["name"] = "Петр"         # изменить значение
person["phone"] = "123-45-67"   # добавить новый ключ
del person["city"]              # удалить ключ
```

## Методы словарей

```python
person.keys()       # dict_keys(['name', 'age', 'city'])
person.values()     # dict_values(['Иван', 14, 'Москва'])
person.items()      # пары ключ-значение

person.get("name")           # "Иван"
person.get("email", "нет")   # "нет" (если ключа нет)
```

## Перебор словаря

```python
for key in person:
    print(key, person[key])

for key, value in person.items():
    print(f"{key}: {value}")
```

---

## 🔧 ФУНКЦИИ

## Создание функции

```python
def greet(name):
    print(f"Привет, {name}!")

greet("Иван")  # Привет, Иван!
```
## Функция с возвращаемым значением

```python
def add(a, b):
    return a + b

result = add(3, 5)  # 8
```

## Функция с параметрами по умолчанию

```python
def greet(name, greeting="Привет"):
    print(f"{greeting}, {name}!")

greet("Иван")              # Привет, Иван!
greet("Мария", "Привет")   # Привет, Мария!
greet("Петр", "Добрый день")  # Добрый день, Петр!
```

## Несколько возвращаемых значений

```python
def get_stats(numbers):
    return max(numbers), min(numbers), sum(numbers)

maximum, minimum, total = get_stats([1, 2, 3, 4, 5])
```

---

## 📂 РАБОТА С ФАЙЛАМИ

## Чтение файла

```python
with open("file.txt", "r", encoding="utf-8") as f:
    content = f.read()  # весь файл в одну строку
    
with open("file.txt", "r", encoding="utf-8") as f:
    lines = f.readlines()  # список строк
```

## Запись в файл

```python
with open("file.txt", "w", encoding="utf-8") as f:
    f.write("Привет, мир!")

# Режимы: "w" — перезаписать, "a" — добавить в конец
```
## Перебор строк файла

```python
with open("file.txt", "r", encoding="utf-8") as f:
    for line in f:
        print(line.strip())
```

---

## 🎲 СЛУЧАЙНЫЕ ЧИСЛА

## Генерация случайных чисел

```python
import random

random.randint(1, 100)      # случайное число от 1 до 100
random.random()             # число от 0.0 до 1.0
random.choice([1, 2, 3])    # выбрать из списка
random.shuffle([1, 2, 3])   # перемешать список
```

---

## 🧮 ПОЛЕЗНЫЕ ФУНКЦИИ

```python
len(x)          # длина строки, списка, словаря
abs(x)          # модуль (абсолютное значение)
round(x, 2)     # округлить до 2 знаков
min(a, b, c)    # минимум из значений
max(a, b, c)    # максимум из значений
sum([1, 2, 3])  # сумма элементов списка
sorted([3, 1, 2])  # отсортировать список
range(n)        # последовательность чисел
```

---

## 🐛 ОТЛАДКА

```python
print(x)              # вывести значение
print(type(x))        # вывести тип
print(f"x = {x}")     # вывести с подписью
```
