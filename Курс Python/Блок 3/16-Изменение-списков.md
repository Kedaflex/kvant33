# Урок 16: Изменение списков

> [!info] 🎯 Цель урока  
> После этого урока ты сможешь добавлять, удалять и изменять элементы списков с помощью методов append()﻿, remove()﻿, pop()﻿.

## 📚 Что понадобится

-  Компьютер с Python
    
-  Предыдущий урок: [[15-Списки-создание|Урок 15: Списки — создание]]
    

---

## 🔥 Разминка (3 мин)

> [!question] Вспомни  
> Как получить элемент списка по индексу? Какой индекс у первого элемента?

<details> <summary>Ответ</summary> Используешь квадратные скобки: [list](pplx://action/translate) для первого элемента, [list[-1](pplx://action/translate)] для последнего. </details>

---

## 📖 Теория (7 мин)

## Что такое методы списка?

Методы — это встроенные функции, которые работают с конкретным типом данных. Для списков есть методы для добавления, удаления и изменения элементов.

Основные методы:

- append()﻿ — добавить элемент в конец
    
- insert()﻿ — добавить элемент на определённую позицию
    
- remove()﻿ — удалить первый элемент с таким значением
    
- pop()﻿ — удалить элемент по индексу (по умолчанию последний)
    

## Синтаксис

```python
lst = [1, 2, 3]

# Добавление
lst.append(4)           # [1, 2, 3, 4]
lst.insert(1, 10)       # [1, 10, 2, 3, 4] (вставить 10 на позицию 1)

# Удаление
lst.remove(2)           # удалить значение 2
lst.pop()               # удалить последний элемент
lst.pop(0)              # удалить первый элемент

# Изменение
lst[0] = 999            # заменить первый элемент на 999

```

## Как это работает?

1. **append()**﻿ — добавляет новый элемент в конец списка, список становится длиннее на 1.
    
2. **insert(index, value)**﻿ — вставляет элемент на нужную позицию, остальные сдвигаются вправо.
    
3. **remove(value)**﻿ — удаляет первое вхождение значения (не индекса!).
    
4. **pop(index)**﻿ — удаляет и возвращает элемент по индексу; если индекс не указан, удаляет последний.
    

> [!warning] Частая ошибка  
> remove()﻿ удаляет по ЗНАЧЕНИЮ (что ты ищешь), а не по индексу. pop()﻿] удаляет по ИНДЕКСУ.

---

## 💻 Практика (15 мин)

## Задание: «Список дел»

**Что делаем:** создаём список дел, добавляем, удаляем и меняем элементы.

## Шаг 1: Создай файл
```text
Создай новый файл: uroк16_izmeneniye_spiska.py
```

## Шаг 2: Напиши код

```python
tasks = ["Учеба", "Спорт", "Компьютер", "Сон"]

print("Исходный список дел:")
print(tasks)
print()

# Добавляем новое дело
tasks.append("Чтение")
print("После добавления 'Чтение':")
print(tasks)
print()

# Вставляем дело на позицию 1
tasks.insert(1, "Завтрак")
print("После вставки 'Завтрак' на позицию 1:")
print(tasks)
print()

# Удаляем 'Компьютер'
tasks.remove("Компьютер")
print("После удаления 'Компьютер':")
print(tasks)
print()

# Удаляем последний элемент
removed = tasks.pop()
print(f"Удалили последний элемент '{removed}':")
print(tasks)
print()

# Меняем первый элемент
tasks[0] = "Математика"
print("После замены первого элемента на 'Математика':")
print(tasks)
```
## Шаг 3: Запусти программу

Следи за изменениями списка на каждом шаге.

## Шаг 4: Создай свой список

Замени дела на свои и проделай минимум 3-4 операции (добавление, удаление, изменение).

## Шаг 5: Добавь интерактивность

Попроси пользователя ввести новое дело и добавь его в список через append()﻿.

text

`Ожидаемый результат: Программа выводит список на каждом этапе и показывает, как он меняется.`

> [!success] Готово!  
> Если ты можешь добавлять, удалять и менять элементы списка, задание выполнено.

---

## ✅ Проверь себя (5 мин)

## Твой код должен:

-  Использовать append()﻿ для добавления
    
-  Использовать insert()﻿ для вставки на позицию
    
-  Использовать remove()﻿ для удаления по значению
    
-  Использовать pop()﻿ для удаления по индексу
    
-  Изменять элементы через прямое присваивание [listi﻿ = value]
    

## Ответь на вопросы:

1. В чём разница между remove()﻿ и pop()﻿?
    
2. Что вернёт pop()﻿, если вызвать его без аргументов?
    

<details> <summary>Ответы</summary>

1. remove(value)﻿ удаляет элемент по ЗНАЧЕНИЮ, pop(index)﻿ удаляет по ИНДЕКСУ и возвращает удалённый элемент.
    
2. pop()﻿] без аргументов удалит и вернёт последний элемент списка.
    

</details>

---

## ⭐ Бонус (если осталось время)

> [!tip] Дополнительное задание  
> Создай программу, которая:
> 
> 1. Создаёт список из 3 чисел
>     
> 2. Спрашивает у пользователя число для добавления и добавляет его
>     
> 3. Спрашивает, какое число удалить, и удаляет его
>     
> 4. Выводит финальный список
>     

---

## 🔗 Навигация

◀️ Предыдущий: [[15-Списки-создание|Урок 15: Списки — создание]]  
▶️ Следующий: [[17-Перебор-списков|Урок 17: Перебор списков]]
🏠 [[00 - Главная|Вернуться к оглавлению]]

---

## 📝 Мои заметки

[Место для заметок ученика]