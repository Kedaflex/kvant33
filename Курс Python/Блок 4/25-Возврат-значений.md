# Урок 25: Возврат значений

> [!info] 🎯 Цель урока  
> После этого урока ты сможешь использовать return﻿ для возврата значений из функции и получать результаты работы функций в переменные.

## 📚 Что понадобится

-  Компьютер с Python
    
-  Предыдущий урок: [[24-Параметры|Урок 24: Параметры]]
    

---

## 🔥 Разминка (3 мин)

> [!question] Вспомни  
> Что происходит, когда функция заканчивает свою работу без return﻿?

<details> <summary>Ответ</summary> Функция просто завершается, но ничего не возвращает (неявно возвращает [None](pplx://action/translate)). </details>

---

## 📖 Теория (7 мин)

## Зачем нужен return?

Без return﻿ функция выполняется, но результат её работы нельзя сохранить в переменную.  
С return﻿ функция возвращает значение, которое можно использовать дальше.

```python
# ❌ Функция только печатает
def add(a, b):
    print(a + b)  # выводит результат, но не возвращает

# ✅ Функция возвращает результат
def add(a, b):
    return a + b  # возвращает число

result = add(5, 3)  # result = 8
print(result * 2)   # 16
```

## Как это работает?

1. return значение﻿ немедленно завершает функцию и возвращает значение вызывающему коду.
    
2. После return﻿ код функции дальше не выполняется.
    
3. Можно возвращать числа, строки, списки, словари — любые типы данных.
    

```python
def max_of_two(a, b):
    if a > b:
        return a
    else:
        return b

biggest = max_of_two(10, 7)
print(biggest)  # 10
```

> [!warning] Частая ошибка  
> Забывать сохранять результат функции в переменную или пытаться использовать результат функции, которая ничего не возвращает.

---

## 💻 Практика (15 мин)

## Задание: «Полезные функции-вычислители»

**Что делаем:** создаём функции, которые возвращают результаты вычислений.

## Шаг 1: Создай файл

```text
Создай новый файл: uroк25_vozvrat_znacheniy.py
```

## Шаг 2: Напиши код

```python
# Функция суммы двух чисел
def add(a, b):
    return a + b

# Функция проверки чётности
def is_even(number):
    return number % 2 == 0

# Функция максимума из трёх чисел
def max_three(a, b, c):
    if a >= b and a >= c:
        return a
    elif b >= a and b >= c:
        return b
    else:
        return c

# Функция создания приветствия
def make_greeting(name, age):
    return f"Привет, {name}! Тебе {age} лет."

# Тестируем функции
sum_result = add(15, 27)
print(f"15 + 27 = {sum_result}")

print(f"10 чётное? {is_even(10)}")
print(f"7 чётное? {is_even(7)}")

print(f"Максимум из 5, 12, 8: {max_three(5, 12, 8)}")

greeting = make_greeting("Иван", 14)
print(greeting)
print(greeting.upper())  # используем результат дальше
```

## Шаг 3: Запусти программу

Убедись, что все функции возвращают правильные значения.

## Шаг 4: Создай свою функцию

Напиши функцию circle_area(radius)﻿, которая возвращает площадь круга: 3.14 * radius ** 2﻿.

## Шаг 5: Используй результат

Вызови свою функцию и используй результат, например, сравни площади двух кругов.

```text
Ожидаемый результат: Программа вызывает функции, сохраняет их результаты и использует дальше.
```

> [!success] Готово!  
> Если ты умеешь возвращать значения из функций и использовать их, return﻿ освоен.

---

## ✅ Проверь себя (5 мин)

## Твой код должен:

-  Иметь минимум 3 функции с return﻿
    
-  Сохранять результаты функций в переменные
    
-  Использовать возвращённые значения в print()﻿ или других вычислениях
    
-  Работать корректно
    

## Ответь на вопросы:

1. Что произойдёт, если вызвать return﻿ в середине функции?
    
2. Можно ли возвращать несколько значений из функции?
    

<details> <summary>Ответы</summary>

1. Функция немедленно завершится и вернёт указанное значение, код после return﻿ не выполнится.
    
2. Да, через кортеж: return value1, value2﻿ или несколько return﻿ в разных ветках.
    

</details>

---

## ⭐ Бонус (если осталось время)

> [!tip] Дополнительное задание  
> Создай функцию get_stats(numbers)﻿, которая принимает список чисел и возвращает три значения: сумму, максимум и минимум. Распакуй результат: [total, maximum, minimum = get_stats((pplx://action/translate))].​[](https://foxford.ru/wiki/informatika/peremennye-v-python)​

---

## 🔗 Навигация

◀️ Предыдущий: [[24-Параметры|Урок 24: Параметры]]  
▶️ Следующий: [[26-Область-видимости|Урок 26: Область видимости]]  
🏠 [[00 - Главная|Вернуться к оглавлению]]

---

## 📝 Мои заметки

[Место для заметок ученика]