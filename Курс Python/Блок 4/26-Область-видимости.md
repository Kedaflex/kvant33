# Урок 26: Область видимости

> [!info] 🎯 Цель урока  
> После этого урока ты поймёшь, где и как работают переменные: локальные (внутри функции) и глобальные (вне функций), и научишься правильно с ними работать.

## 📚 Что понадобится

-  Компьютер с Python
    
-  Предыдущий урок: [[25-Возврат-значений|Урок 25: Возврат значений]]
    

---

## 🔥 Разминка (3 мин)

> [!question] Вспомни  
> Если функция возвращает значение через return﻿, как его сохранить и использовать?

<details> <summary>Ответ</summary> Сохраняем в переменную: [result = func_name(arguments)](pplx://action/translate), потом используем [result](pplx://action/translate). </details>

---

## 📖 Теория (7 мин)

## Что такое область видимости?

**Область видимости** — это зона программы, в которой переменная «видна» и доступна.  
Есть два типа:

- **Глобальная** — переменная вне функций, видна везде
    
- **Локальная** — переменная внутри функции, видна только внутри неё
    

```python
global_var = 10  # глобальная переменная

def my_function():
    local_var = 5   # локальная переменная
    print(global_var)  # можем читать глобальную
    print(local_var)   # можем читать локальную

my_function()
print(global_var)    # можем читать глобальную снаружи
# print(local_var)  # ОШИБКА! local_var не видна снаружи
```

## Правила:

1. **Локальные** переменные создаются внутри функции и существуют только там
    
2. **Глобальные** переменные видны везде, но их лучше не менять внутри функций
    
3. Функция может **читать** глобальные переменные, но для **изменения** нужен global﻿
    

> [!warning] Частая ошибка  
> Создавать переменную внутри функции и пытаться использовать её снаружи — она там не видна!

---

## 💻 Практика (15 мин)

## Задание: «Локальные и глобальные переменные»

**Что делаем:** экспериментируем с областями видимости.

## Шаг 1: Создай файл

```text
Создай новый файл: uroк26_oblast_vidimosti.py
```
## Шаг 2: Напиши код

```python
# Глобальная переменная
counter = 0

def increment():
    global counter  # объявляем, что работаем с глобальной
    counter += 1
    local_count = 10  # локальная переменная
    print(f"Внутри функции: counter={counter}, local_count={local_count}")

def just_print():
    print(f"Глобальная counter снаружи функции: {counter}")
    # print(local_count)  # ОШИБКА! local_count не видна

print("До вызова функций, counter =", counter)

increment()
increment()
just_print()

print("После вызовов, counter =", counter)
```

## Шаг 3: Запусти программу

Следи, как меняется counter﻿ и что происходит с local_count﻿.

## Шаг 4: Создай свою функцию

Напиши функцию safe_calculate(a, b)﻿, которая возвращает сумму, но НЕ меняет глобальные переменные.

## Шаг 5: Сравни подходы

Покажи разницу между функцией, которая возвращает результат, и функцией, которая меняет глобальную переменную.

text

`Ожидаемый результат: Программа показывает, как работают локальные и глобальные переменные.`

> [!success] Готово!  
> Если ты понимаешь разницу между областями видимости и умеешь с ними работать, тема освоена.

---

## ✅ Проверь себя (5 мин)

## Твой код должен:

-  Показывать глобальную переменную внутри и снаружи функции
    
-  Создавать локальную переменную внутри функции
    
-  Демонстрировать ошибку при попытке доступа к локальной переменной снаружи
    
-  Использовать global﻿ для изменения глобальной переменной
    

## Ответь на вопросы:

1. Где создаётся локальная переменная и где она доступна?
    
2. Зачем нужен global переменная﻿ внутри функции?
    

<details> <summary>Ответы</summary>

1. Локальная переменная создаётся внутри функции и доступна только внутри неё.
    
2. global﻿ говорит Python, что нужно изменить именно глобальную переменную, а не создать новую локальную с таким же именем.
    

</details>

---

## ⭐ Бонус (если осталось время)

> [!tip] Дополнительное задание  
> Создай функцию update_score(name, points)﻿, которая обновляет глобальный словарь оценок: [scoresname﻿ += points]. Протестируй на нескольких игроках.

---

## 🔗 Навигация

◀️ Предыдущий: [[25-Возврат-значений|Урок 25: Возврат значений]]  
▶️ Следующий: [[27-Библиотека-функций|Урок 27: Практика — Библиотека функций]]  
🏠 [[00 - Главная|Вернуться к оглавлению]]

---

## 📝 Мои заметки

[Место для заметок ученика]