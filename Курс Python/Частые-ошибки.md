# ⚠️ Частые ошибки в Python

> [!warning] Важно!  
> Эта страница собирает самые частые ошибки, которые делают ученики. Если что-то не работает — посмотри сюда в первую очередь!

---

## 🔴 СИНТАКСИЧЕСКИЕ ОШИБКИ

## Ошибка 1: Забыл двоеточие после if, for, while

```python
# ❌ НЕПРАВИЛЬНО
if age > 18
    print("Взрослый")

# ✅ ПРАВИЛЬНО
if age > 18:
    print("Взрослый")
```

**Симптом:** SyntaxError: invalid syntax﻿  
**Решение:** Всегда ставь двоеточие :﻿ после if﻿, else﻿, elif﻿, for﻿, while﻿, def﻿.

---

## Ошибка 2: Неправильный отступ (indentation)

```python
# ❌ НЕПРАВИЛЬНО
for i in range(5):
print(i)  # отступа нет!

# ✅ ПРАВИЛЬНО
for i in range(5):
    print(i)  # 4 пробела или Tab
```

**Симптом:** IndentationError: expected an indented block﻿  
**Решение:** Используй ровно 4 пробела (или Tab) внутри блоков кода.

---

## Ошибка 3: Путаешь = и ==

```python
# ❌ НЕПРАВИЛЬНО (присваивание вместо сравнения)
if age = 18:
    print("Совершеннолетний")

# ✅ ПРАВИЛЬНО
if age == 18:
    print("Совершеннолетний")
````

**Симптом:** SyntaxError﻿ или неправильное поведение  
**Решение:** =﻿ присваивает значение, ==﻿ сравнивает.

---

## 🟡 ЛОГИЧЕСКИЕ ОШИБКИ

## Ошибка 4: Бесконечный цикл

```python
# ❌ НЕПРАВИЛЬНО
count = 0
while count < 10:
    print("Привет")
    # забыли увеличить count!

# ✅ ПРАВИЛЬНО
count = 0
while count < 10:
    print("Привет")
    count += 1  # увеличиваем счётчик

```

**Симптом:** Программа зависает, ничего не выводит или выводит бесконечно  
**Решение:** Убедись, что в цикле изменяется переменная, от которой зависит условие.

---

## Ошибка 5: range() не включает верхнюю границу

```python
# ❌ НЕПРАВИЛЬНО
age = 14
message = "Мне " + age + " лет"  # error!

# ✅ ПРАВИЛЬНО (вариант 1)
age = 14
message = "Мне " + str(age) + " лет"

# ✅ ПРАВИЛЬНО (вариант 2)
age = 14
message = f"Мне {age} лет"

```

**Симптом:** Программа не выводит нужное число  
**Решение:** range(start, stop)﻿ выводит от start﻿ до stop-1﻿. Добавь 1 к верхней границе!

---

## 🔵 ОШИБКИ ТИПОВ ДАННЫХ

## Ошибка 6: Смешиваешь строку и число

```python
# ❌ НЕПРАВИЛЬНО
age = 14
message = "Мне " + age + " лет"  # error!

# ✅ ПРАВИЛЬНО (вариант 1)
age = 14
message = "Мне " + str(age) + " лет"

# ✅ ПРАВИЛЬНО (вариант 2)
age = 14
message = f"Мне {age} лет"
```
**Симптом:** TypeError: can only concatenate str (not "int") to str﻿  
**Решение:** Либо преобразуй число в строку через str()﻿, либо используй f-строку.

---

## Ошибка 7: Забыл преобразовать input в число

```python
# ❌ НЕПРАВИЛЬНО
age = input("Твой возраст: ")
next_year = age + 1  # error! age — это строка "14", не число 14

# ✅ ПРАВИЛЬНО
age = int(input("Твой возраст: "))
next_year = age + 1
```

**Симптом:** TypeError: can only concatenate str (not "int") to str﻿ или неправильный результат  
**Решение:** Всегда преобразуй input()﻿ в нужный тип: int()﻿, float()﻿ или оставь как str﻿().

---

## Ошибка 8: Используешь неправильный тип для операции

```python
# ❌ НЕПРАВИЛЬНО
number = "123"
result = number + 5  # error! число нельзя прибавить к строке

# ✅ ПРАВИЛЬНО
number = int("123")
result = number + 5  # 128
````

**Симптом:** TypeError﻿  
**Решение:** Проверь типы переменных через type(variable)﻿.

---

## 🟣 ОШИБКИ ИМЁН И ПЕРЕМЕННЫХ

## Ошибка 9: Используешь переменную, которую не определил

```python
# ❌ НЕПРАВИЛЬНО
print(name)  # ошибка! переменная name не существует

# ✅ ПРАВИЛЬНО
name = "Иван"
print(name)
````

**Симптом:** NameError: name 'name' is not defined﻿  
**Решение:** Всегда определи переменную перед использованием.

---

## Ошибка 10: Запутался в области видимости переменной

```python
# ❌ НЕПРАВИЛЬНО
for i in range(5):
    x = i

print(x)  # это работает! x видна снаружи цикла (в Python это особенность)

# ⚠️ ПРАВИЛЬНЕЕ (для понимания)
for i in range(5):
    x = i

if x > 0:
    print(f"x = {x}")
```
**Симптом:** Неожиданное значение переменной  
**Решение:** Помни: в Python переменные, определённые в цикле, видны снаружи.

---

## 🟠 ОШИБКИ УСЛОВИЙ

## Ошибка 11: Забыл проверить граничные значения

```python
# ❌ НЕПРАВИЛЬНО (не обработал случай age == 18)
if age > 18:
    print("Взрослый")
else:
    print("Подросток")

# ✅ ПРАВИЛЬНО
if age >= 18:
    print("Взрослый")
else:
    print("Подросток")
```

**Симптом:** Программа не работает на граничных значениях (18, 0, пустая строка)  
**Решение:** Думай о граничных случаях: что, если возраст ровно 18? Что, если число 0?

---

## Ошибка 12: Используешь =﻿ вместо ==﻿ в условии

```python
# ❌ НЕПРАВИЛЬНО
if x = 5:
    print("x равен 5")

# ✅ ПРАВИЛЬНО
if x == 5:
    print("x равен 5")
```

**Симптом:** SyntaxError﻿  
**Решение:** В условиях всегда используй ==﻿ для сравнения, =﻿ только для присваивания.

---

## 🔴 ОШИБКИ ЦИКЛОВ

## Ошибка 13: Забыл break и попал в бесконечный цикл

```python
# ❌ НЕПРАВИЛЬНО
while True:
    answer = input("Хочешь ещё раз? (да/нет): ")
    if answer == "да":
        print("Продолжаем!")
    # забыли break при ответе "нет"!

# ✅ ПРАВИЛЬНО
while True:
    answer = input("Хочешь ещё раз? (да/нет): ")
    if answer == "да":
        print("Продолжаем!")
    elif answer == "нет":
        print("Конец!")
        break
```
**Симптом:** Программа не заканчивается  
**Решение:** Если используешь while True﻿, обязательно добавь break﻿ где-то!

---

## Ошибка 14: Путаешь continue и break

```python
# ❌ НЕПРАВИЛЬНО (используешь break, а нужен continue)
for i in range(10):
    if i == 5:
        break  # весь цикл прерывается!
    print(i)  # выводит 0-4, потом ничего

# ✅ ПРАВИЛЬНО
for i in range(10):
    if i == 5:
        continue  # пропускаем 5 и идём дальше
    print(i)  # выводит 0-4, 6-9

```
**Симптом:** Программа выходит из цикла раньше времени  
**Решение:** break﻿ — выход из цикла, continue﻿ — пропуск текущей итерации.

---

## 💡 ПОЛЕЗНЫЕ СОВЕТЫ

## Как отлаживать ошибку?

1. **Читай сообщение об ошибке** — оно подскажет тип ошибки и строку.
    
2. **Выведи значение переменной** — используй print()﻿, чтобы посмотреть, что в переменной.
    
3. **Проверь типы** — используй type(variable)﻿.
    
4. **Проверь логику** — пройди программу «руками» шаг за шагом.
    

## Пример отладки:

python

`age = input("Возраст: ") print(f"age = {age}, тип = {type(age)}")  # смотрим значение и тип next_year = age + 1  # ошибка! age строка, а не число`

---

## 🆘 Самые частые ошибки по урокам

|Урок|Ошибка|Решение|
|---|---|---|
|1-2|Забыл кавычки в строке|Оборачивай текст в кавычки|
|3-4|Смешал типы данных|Используй int()﻿, str()﻿, float()|
|5|Неправильный порядок операций|Помни: ** → *, / → +, -|
|7|Спутал =﻿ и ==|=﻿ присваивает, ==﻿ сравнивает|
|9-10|Бесконечный цикл|Не забывай увеличивать/уменьшать счётчик|
|11|Путает break﻿ и continue|break﻿ выход, continue﻿ пропуск|

---

## 📝 Твоя чек-лист перед запуском программы

-  Все if﻿, for﻿, while﻿ заканчиваются двоеточием :﻿ ?
    
-  Все блоки кода внутри них имеют отступ?
    
-  Все переменные определены перед использованием?
    
-  Все input()﻿ преобразованы в правильный тип?
    
-  Используешь ==﻿ для сравнения, а не =﻿ ?
    
-  В циклах изменяется переменная, от которой зависит условие?
    
-  Если while True﻿, есть ли break﻿ ?
    

---

## 🔗 Навигация

🏠 [[00 - Главная|Вернуться к оглавлению]]

---

**Помни:** Ошибки — это нормально! Каждый программист их делает. Главное — научиться их находить и исправлять. 🚀