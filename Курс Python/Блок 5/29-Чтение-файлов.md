# Урок 29: Чтение файлов

> [!info] 🎯 Цель урока  
> После этого урока ты сможешь открывать текстовые файлы в Python, читать их содержимое целиком или построчно с помощью open()﻿, read()﻿, readline()﻿, readlines()﻿ и конструкции with﻿.[](https://www.w3schools.com/python/python_file_open.asp)​

## 📚 Что понадобится

-  Компьютер с Python
    
-  Текстовый файл data.txt﻿ в той же папке, что и программа
    
-  Предыдущий урок: [[28-Контрольная-4|Урок 28: Контрольная работа]]
    

---

## 🔥 Разминка (3 мин)

> [!question] Вспомни  
> Какая функция в Python используется для чтения данных с клавиатуры и чем она отличается от чтения из файла?

<details> <summary>Ответ</summary> Для клавиатуры используется [input()](pplx://action/translate), она ждёт ввод пользователя во время работы программы. Файлы читаются функцией [open()](pplx://action/translate) и методами файла — данные уже сохранены на диске.[4][5] </details>

---

## 📖 Теория (7 мин)

## Как открыть файл?

Файлы открываются встроенной функцией open("имя_файла", "режим")﻿.  
Для чтения текста используют режим "r"﻿ (read).[](https://www.freecodecamp.org/news/file-handling-in-python/)​

```python
f = open("data.txt", "r")   # открыть файл для чтения
content = f.read()          # прочитать всё содержимое
f.close()                   # закрыть файл
```
Лучше использовать конструкцию with﻿ — файл закроется автоматически:[](https://www.pyquantnews.com/free-python-resources/file-handling-in-python-a-comprehensive-guide)​

```python
with open("data.txt", "r", encoding="utf-8") as f:
    content = f.read()
    print(content)

```

## Способы чтения

```python
with open("data.txt", "r", encoding="utf-8") as f:
    all_text = f.read()        # строка со всем содержимым файла

with open("data.txt", "r", encoding="utf-8") as f:
    first_line = f.readline()  # одна строка

with open("data.txt", "r", encoding="utf-8") as f:
    lines = f.readlines()      # список строк

```

Также можно читать файл в цикле построчно:
```python
with open("data.txt", "r", encoding="utf-8") as f:
    for line in f:
        print(line.strip())
```

> [!warning] Частая ошибка  
> Забывать закрывать файл (f.close()﻿) или неправильно указывать путь/имя файла — тогда Python не найдёт файл.[](https://www.geeksforgeeks.org/python/how-to-read-from-a-file-in-python/)​

---

## 💻 Практика (15 мин)

## Задание: «Чтение файла с заметками»

**Что делаем:** читаем текст из файла, выводим его целиком и построчно.

## Шаг 1: Подготовь файл

Создай рядом с программой текстовый файл:

```text
Создай файл: data.txt Запиши в него 3–5 строк любого текста (например, заметки, цитаты, список дел).
```

## Шаг 2: Создай файл программы
```text
Создай новый файл: uroк29_read_file.py
```

## Шаг 3: Напиши код

```python
# Читаем весь файл целиком
print("=== Чтение всего файла ===")
with open("data.txt", "r", encoding="utf-8") as f:
    content = f.read()
    print(content)

print("\n=== Чтение построчно через readline() ===")
with open("data.txt", "r", encoding="utf-8") as f:
    line1 = f.readline()
    line2 = f.readline()
    print("Строка 1:", line1.strip())
    print("Строка 2:", line2.strip())

print("\n=== Чтение всех строк в список ===")
with open("data.txt", "r", encoding="utf-8") as f:
    lines = f.readlines()
    print("Всего строк:", len(lines))
    for i, line in enumerate(lines, start=1):
        print(f"Строка {i}: {line.strip()}")
```
## Шаг 4: Запусти программу

Проверь, что:

- текст файла выводится целиком
    
- первые две строки читаются отдельно
    
- все строки отображаются с номерами
    

## Шаг 5: Измени файл

Добавь ещё пару строк в data.txt﻿ и снова запусти программу, посмотри, как изменится вывод.

```text
Ожидаемый результат: Программа демонстрирует разные способы чтения файла: целиком, по строкам, в список.
```

> [!success] Готово!  
> Если файл читается без ошибок и вывод соответствует содержимому, базовое чтение файлов освоено.

---

## ✅ Проверь себя (5 мин)

## Твой код должен:

-  Использовать with open(..., "r", encoding="utf-8")﻿ для чтения файла
    
-  Использовать минимум два способа чтения (read()﻿, readline()﻿ или readlines()﻿)[](https://www.w3schools.com/python/ref_file_readlines.asp)​
    
-  Корректно обрабатывать строки (например, через strip()﻿)
    
-  Работать только с существующим файлом в той же папке
    

## ## Ответь на вопросы:

1. Зачем нужен with open(...)﻿ вместо open()﻿ и close()﻿ вручную?
    
2. Чем отличается read()﻿ от readlines()﻿?
    

<details> <summary>Ответы</summary>

1. with﻿ автоматически закрывает файл даже при ошибках, код становится безопаснее и короче.[](https://www.codecademy.com/article/handling-text-files-in-python)​
    
2. read()﻿ возвращает всю информацию как одну строку, а readlines()﻿ возвращает список строк файла.[](https://www.geeksforgeeks.org/python/read-a-file-line-by-line-in-python/)​
    

</details>

---

## ⭐ Бонус (если осталось время)

> [!tip] Дополнительное задание  
> Добавь в конец программы подсчёт количества непустых строк в файле (игнорируй строки, где после strip()﻿ получилось пусто).

---

## 🔗 Навигация

◀️ Предыдущий: [[28-Контрольная-4|Урок 28: Контрольная работа]]  
▶️ Следующий: [[30-Запись-в-файлы|Урок 30: Запись в файлы]]  
🏠 [[00 - Главная|Вернуться к оглавлению]]

---

## 📝 Мои заметки

[Место для заметок ученика]