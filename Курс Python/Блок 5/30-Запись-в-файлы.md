# Урок 30: Запись в файлы

> [!info] 🎯 Цель урока  
> После этого урока ты сможешь создавать и изменять текстовые файлы в Python с помощью режимов записи "w"﻿ и добавления "a"﻿, а также методов write()﻿ и writelines()﻿.[](https://stackoverflow.com/questions/4706499/how-do-i-append-to-a-file)​

## 📚 Что понадобится

-  Компьютер с Python
    
-  Любой текстовый редактор
    
-  Предыдущий урок: [[29-Чтение-файлов|Урок 29: Чтение файлов]]
    

---

## 🔥 Разминка (3 мин)

> [!question] Вспомни  
> В каком режиме открывается файл для чтения и какая буква указывает этот режим?

<details> <summary>Ответ</summary> Файл для чтения открывается в режиме ["r"](pplx://action/translate) — от слова [read](pplx://action/translate).[4][5] </details>

---

## 📖 Теория (7 мин)

## Режимы записи

При работе с файлами важен **режим**:[](https://www.geeksforgeeks.org/python/writing-to-file-in-python/)​

- "w"﻿ — запись (write)
    
    - создаёт новый файл, если его нет
        
    - очищает существующий файл и записывает заново
        
- "a"﻿ — добавление (append)
    
    - создаёт файл, если его нет
        
    - дописывает в конец, не стирая старый текст
        

```python
# Перезаписать файл
with open("log.txt", "w", encoding="utf-8") as f:
    f.write("Первая строка\n")

# Добавить в конец файла
with open("log.txt", "a", encoding="utf-8") as f:
    f.write("Ещё одна строка\n")
```

## write() и writelines()

```python
lines = ["Первая строка\n", "Вторая строка\n", "Третья строка\n"]

with open("notes.txt", "w", encoding="utf-8") as f:
    f.writelines(lines)   # записывает список строк как есть
```
Важно: write()﻿ и writelines()﻿ не добавляют перевод строки сами, нужно вставлять "\n"﻿ в текст.[](https://www.w3schools.com/python/ref_file_writelines.asp)​

> [!warning] Частая ошибка  
> Открывать файл в режиме "w"﻿, когда нужно только добавить строку — при этом старое содержимое полностью стирается. Для добавления используй "a"﻿.[](https://apxml.com/courses/python-for-beginners/chapter-6-interacting-with-files/python-file-modes)​

---

## 💻 Практика (15 мин)

## Задание: «Дневник заметок»

**Что делаем:** создаём файл заметок и учимся в него записывать и дописывать строки.

## Шаг 1: Создай файл программы

```text
Создай новый файл: uroк30_write_file.py
```

## Шаг 2: Напиши код (перезапись файла)

```python
print("=== Перезаписываем файл notes.txt ===")

with open("notes.txt", "w", encoding="utf-8") as f:
    f.write("Моя первая заметка\n")
    f.write("Сегодня я учусь записывать файлы в Python.\n")

print("Файл notes.txt создан и перезаписан.")
```

Запусти программу и открой notes.txt﻿ в редакторе — убедись, что там ровно 2 строки.

## Шаг 3: Добавь код для append

Добавь в тот же файл программы ниже:

```python
print("\n=== Добавляем строки в конец файла notes.txt ===")

with open("notes.txt", "a", encoding="utf-8") as f:
    f.write("Эта строка добавлена позже.\n")
    f.write("Python — это интересно!\n")

print("Новые строки добавлены в конец файла.")
```

Запусти программу второй раз и проверь, что файл **не стёрся**, а строки дописались.

## Шаг 4: Используй список строк

Добавь ещё блок:
```python
print("\n=== Добавляем список строк через writelines() ===")

extra_lines = [
    "Строка из списка 1\n",
    "Строка из списка 2\n",
    "Строка из списка 3\n"
]

with open("notes.txt", "a", encoding="utf-8") as f:
    f.writelines(extra_lines)

print("Строки из списка добавлены.")
```

## Шаг 5: Прочитай файл

В конце файла программы прочитай notes.txt﻿ и выведи содержимое:

```python
print("\n=== Итоговое содержимое файла notes.txt ===")
with open("notes.txt", "r", encoding="utf-8") as f:
    print(f.read())
```

```text
Ожидаемый результат: Файл notes.txt сначала создаётся и перезаписывается, затем в него добавляются новые строки, и в конце на экран выводится всё содержимое.
```

> [!success] Готово!  
> Если видишь, как файл меняется и дополняется, запись в файлы работает как надо.

---

## ✅ Проверь себя (5 мин)

## Твой код должен:

-  Открывать файл в режиме "w"﻿ для перезаписи
    
-  Открывать файл в режиме "a"﻿ для добавления строк[](https://betterstack.com/community/questions/how-to-append-to-a-file-in-python/)​
    
-  Использовать write()﻿ и/или writelines()﻿ с символами перевода строки "\n"﻿
    
-  В конце читать файл и показывать итоговое содержимое
    

## Ответь на вопросы:

1. В чём разница между режимами "w"﻿ и "a"﻿?
    
2. Что произойдёт, если открыть существующий файл в режиме "w"﻿ и ничего не записать?
    

<details> <summary>Ответы</summary>

1. "w"﻿ перезаписывает файл (стирает старое содержимое), "a"﻿ дописывает в конец, не стирая существующий текст.[](https://www.geeksforgeeks.org/python/file-mode-in-python/)​
    
2. Файл будет очищен (его размер станет 0 байт), содержимое исчезнет.[](https://www.codecademy.com/article/handling-text-files-in-python)​
    

</details>

---

## ⭐ Бонус (если осталось время)

> [!tip] Дополнительное задание  
> Сделай «журнал запусков»: каждый раз при запуске программы открывай log.txt﻿ в режиме "a"﻿ и дописывай туда строку вида:  
> "Программа запущена."﻿  
> Потом выведи на экран, сколько всего строк в логе.

---

## 🔗 Навигация

◀️ Предыдущий: [[29-Чтение-файлов|Урок 29: Чтение файлов]]  
▶️ Следующий: [[31-Викторина-1|Урок 31: Проект — Викторина (часть 1)]]  
🏠 [[00 - Главная|Вернуться к оглавлению]]

---

## 📝 Мои заметки

[Место для заметок ученика]