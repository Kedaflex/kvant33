# Урок 32: Проект — Викторина (часть 2)

> [!info] 🎯 Цель урока  
> После этого урока ты добавишь в викторину подсчёт очков, сохранение лучшего результата в файл и улучшенное оформление игры.[](https://stackoverflow.com/questions/16726354/saving-the-highscore-for-a-game)​

## 📚 Что понадобится

-  Компьютер с Python
    
-  Файл с вопросами: questions.txt﻿ (из урока 31)
    
-  Предыдущий урок: [[31-Викторина-1|Урок 31: Проект — Викторина (часть 1)]]
    

---

## 🔥 Разминка (3 мин)

> [!question] Вспомни  
> Как сохранить данные в файл так, чтобы при следующем запуске программы их можно было прочитать?

<details> <summary>Ответ</summary> Нужно открыть файл в режиме ["w"](pplx://action/translate) или ["a"](pplx://action/translate), записать данные через [write()](pplx://action/translate) или [writelines()](pplx://action/translate), закрыть файл, а потом при следующем запуске открыть его в режиме ["r"](pplx://action/translate) и прочитать.[4][5][6] </details>

---

## 📖 Теория (7 мин)

## Лучший результат (рекорд)

Идея:

- Хранить лучший результат (рекорд) в отдельном файле, например highscore.txt﻿.
    
- При старте игры прочитать рекорд (если файла нет — считать рекорд 0).[](https://projectai.in/projects/25e96c08-f34e-4f04-924c-1426afac5de3/tasks/15d31df3-78c5-47ab-99c6-bf4e7f753aed)​
    
- После игры, если текущий счёт больше рекорда, записать новый рекорд в файл.
    

Простой формат файла: внутри одно число, например:

text

`5`

## Алгоритм

1. Попробовать открыть highscore.txt﻿ и прочитать число.
    
2. Если файла нет или он пустой — рекорд = 0.
    
3. Провести викторину, посчитать score﻿.
    
4. Сравнить score﻿ и highscore﻿.
    
5. Если score > highscore﻿, записать новый рекорд в файл.​[](https://stackoverflow.com/questions/16726354/saving-the-highscore-for-a-game)​
    

> [!warning] Частая ошибка  
> Пытаться преобразовать пустую строку в число или не обрабатывать ситуацию, когда файла рекорда ещё нет. Нужен try/except﻿ или проверка.[](https://www.reddit.com/r/pygame/comments/c66x0q/saving_the_high_score_on_a_file/)​

---

## 💻 Практика (15 мин)

## Задание: «Викторина с рекордом»

**Что делаем:** дорабатываем викторину из урока 31 — добавляем рекорд и подсказки.

## Шаг 1: Подготовь файл рекорда

Создай пустой файл или файла может не быть — программа сама создаст:

```text
Имя файла: highscore.txt Содержимое (по желанию): 0
```

## Шаг 2: Создай файл программы (новая версия викторины)

```text
Создай новый файл: uroк32_quiz_part2.py
```

## Шаг 3: Напиши код

```python
QUESTIONS_FILE = "questions.txt"
HIGHSCORE_FILE = "highscore.txt"

def load_questions(filename):
    with open(filename, "r", encoding="utf-8") as f:
        lines = [line.strip() for line in f.readlines()]

    qa_list = []
    for i in range(0, len(lines), 2):
        if i + 1 < len(lines):
            question = lines[i]
            answer = lines[i + 1]
            qa_list.append((question, answer))
    return qa_list

def load_highscore(filename):
    try:
        with open(filename, "r", encoding="utf-8") as f:
            text = f.read().strip()
            if text == "":
                return 0
            return int(text)
    except FileNotFoundError:
        return 0

def save_highscore(filename, score):
    with open(filename, "w", encoding="utf-8") as f:
        f.write(str(score))

# --- Основная программа ---

questions = load_questions(QUESTIONS_FILE)
highscore = load_highscore(HIGHSCORE_FILE)

print("=== ВИКТОРИНА 2.0 ===")
print(f"Всего вопросов: {len(questions)}")
print(f"Текущий рекорд: {highscore}\n")

score = 0

for question, right_answer in questions:
    print(question)
    user_answer = input("Твой ответ: ").strip()

    if user_answer.lower() == right_answer.lower():
        print("✔ Правильно!\n")
        score += 1
    else:
        print(f"✖ Неправильно. Правильный ответ: {right_answer}\n")

print(f"Твой результат: {score} из {len(questions)}")

if score > highscore:
    print("🎉 НОВЫЙ РЕКОРД! Поздравляем!")
    save_highscore(HIGHSCORE_FILE, score)
else:
    print(f"Рекорд пока: {highscore}")
```

## Шаг 4: Запусти программу несколько раз

- Один раз — ответь на пару вопросов неправильно
    
- Потом попробуй набрать максимальный результат
    
- Убедись, что highscore.txt﻿ обновляется и рекорд сохраняется между запусками
    

## Шаг 5: Добавь оценку игрока

После итогового результата выведи комментарий:

- если набрал все — "Отлично!"﻿
    
- если больше половины — "Хороший результат."﻿
    
- иначе — "Нужно потренироваться."﻿
    

```text
Ожидаемый результат: Викторина показывает старый рекорд, позволяет играть, считает очки и сохраняет новый рекорд в файл при его побитии. [web:197][web:210][web:212]
```

> [!success] Готово!  
> Если рекорд сохраняется между запусками и викторина стала «игрой с прогрессом», часть 2 проекта завершена.

---

## ✅ Проверь себя (5 мин)

## Твой код должен:

-  Читать вопросы и ответы из файла
    
-  Читать рекорд из отдельного файла (или считать 0, если файла нет)
    
-  Записывать новый рекорд, если текущий результат выше[](https://stackoverflow.com/questions/16726354/saving-the-highscore-for-a-game)​
    
-  Корректно работать при повторных запусках программы
    

## Ответь на вопросы:

1. Почему рекорд нужно хранить в отдельном файле, а не только в переменной?
    
2. Что будет, если удалить highscore.txt﻿ и снова запустить программу?
    

<details> <summary>Ответы</summary>

1. Переменные живут только во время работы программы; файл позволяет сохранить рекорд между запусками.​[](https://stackoverflow.com/questions/16726354/saving-the-highscore-for-a-game)​
    
2. Программа должна создать новый рекорд с 0 (или с текущим результатом), потому что при отсутствии файла мы считаем рекорд равным 0.[](https://stackoverflow.com/questions/16726354/saving-the-highscore-for-a-game)​
    

</details>

---

## ⭐ Бонус (если осталось время)

> [!tip] Дополнительное задание  
> Добавь в файл highscore.txt﻿ не только лучший результат, но и имя игрока, например: "Иван: 7"﻿. При новом рекорде спрашивай имя и обновляй строку.

---

## 🔗 Навигация

◀️ Предыдущий: [[31-Викторина-1|Урок 31: Проект — Викторина (часть 1)]]  
▶️ Следующий: [[33-Текстовый-квест|Урок 33: Проект — Текстовый квест]]  
🏠 [[00 - Главная|Вернуться к оглавлению]]

---

## 📝 Мои заметки

[Место для заметок ученика]